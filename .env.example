# Local Supabase keys for docker-compose (supabase start).
# Copy to .env and fill with the values printed by `supabase status`.
# These only target the local instance (127.0.0.1).
SUPABASE_ANON_KEY=sb_publishable_CHANGE_ME
SUPABASE_SERVICE_ROLE_KEY=sb_secret_CHANGE_ME
